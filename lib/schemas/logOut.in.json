{
    "type": "object",
    "properties": {
        "apiSecret": {
            "required": true,
            "type": "string",
            "pattern": "^[a-zA-Z0-9]{19,50}$"
        },
        "tgt": {
            "required": true,
            "type": "string",
            "pattern": "^[a-zA-Z][a-zA-Z0-9-]{39,100}$"
        }
    }
}